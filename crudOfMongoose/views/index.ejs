<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <div class="flex w-full h-full">
      <div
        class="w-1/2 h-full bg-zinc-900 p-10 border mr-1 rounded-lg shadow-lg"
      >
        <form action="/create" method="post">
          <div class="mb-4">
            <input
              type="text"
              name="name"
              maxlength="25"
              class="outline-none w-full p-3 bg-zinc-600 text-white rounded-lg placeholder-gray-400 focus:outline-none"
              placeholder="Name..."
              required
            />
          </div>
          <div class="mb-4">
            <input
              type="text"
              name="address"
              maxlength="25"
              class="outline-none w-full p-3 bg-zinc-600 text-white rounded-lg placeholder-gray-400 focus:outline-none"
              placeholder="Address..."
              required
            />
          </div>
          <div class="mb-4">
            <input
              type="tel"
              maxlength="10"
              name="phoneNo"
              class="outline-none w-full p-3 bg-zinc-600 text-white rounded-lg placeholder-gray-400 focus:outline-none"
              placeholder="Phone Number..."
              required
            />
          </div>
          <div class="flex w-full mt-4 justify-center">
            <input
              type="submit"
              value="Create User"
              class="py-2 px-4 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors duration-300"
            />
          </div>
        </form>
        <% if (id!==null) { %>         
        <% if (user) { %>

        <div>
          <div
            class="w-full max-w-md mx-auto p-4 bg-gray-800 rounded-lg shadow-lg text-white mt-5"
          >
          <header class="header text-2xl text-white mb-3">Change User Info.</header>
            <form action="/edit/<%=id%>" method="post" class="space-y-4">
              <div>
                <label for="name" class="block mb-2 text-sm font-medium"
                  >Name</label
                >
                <input
                  type="text"
                  maxlength="25"
                  name="name"
                  value="<%= user.name %>"
                  class="w-full px-4 py-2 bg-gray-700 border border-gray-600 rounded-lg outline-none focus:border-blue-500"
                  id="name"
                  required
                />
              </div>
              <div>
                <label for="address" class="block mb-2 text-sm font-medium"
                  >Address</label
                >
                <input
                  type="text"
                  maxlength="25"
                  name="address"
                  value="<%= user.address %>"
                  class="w-full px-4 py-2 bg-gray-700 border border-gray-600 rounded-lg outline-none focus:border-blue-500"
                  id="address"
                  required
                />
              </div>
              <div>
                <label for="phoneNo" class="block mb-2 text-sm font-medium"
                  >Phone Number</label
                >
                <input
                  type="tel"
                  maxlength="10"
                  name="phoneNo"
                  value="<%= user.phoneNo %>"
                  class="w-full px-4 py-2 bg-gray-700 border border-gray-600 rounded-lg outline-none focus:border-blue-500"
                  id="phoneNo"
                  required
                />
              </div>
              <div>
                <input
                  type="submit"
                  value="Save"
                  class="w-full py-2 bg-blue-500 hover:bg-blue-700 text-white font-semibold rounded-lg cursor-pointer transition duration-200"
                />
              </div>
            </form>
          </div>
        </div>

        <% } %>
        <% } %>
      </div>
      <div
        class="w-1/2 h-[260px] py-10 px-6 ml-1 grid grid-cols-1 md:grid-cols-2 gap-4"
      >
        <% if(users.length>0) { %> <% users.forEach((sUser)=>{ %>
        <div class="card bg-blue-500 rounded-lg shadow-lg p-6 pb-0">
          <h3 class="text-white bg-gray-900 rounded p-2 mb-2">
            Name: <code><%= sUser.name %></code>
          </h3>
          <h4 class="text-white bg-gray-900 rounded p-2 mb-2">
            Address: <code><i><%= sUser.address %></i></code>
          </h4>
          <h4 class="text-white bg-gray-900 rounded p-2">
            PhoneNo: <code><%= sUser.phoneNo %></code>
          </h4>
          <div class="justify-center mx-auto my-3 items-center text-white">
            <a
              href="/edit/<%=sUser._id%>"
              class="mx-2 px-2 bg-green-500 rounded-md text-1xl"
              >Edit</a
            >
            <a href="/delete/<%=sUser._id%>" class="mx-2 px-2 bg-red-500 rounded-md text-1xl"
              >Delete</a
            >
          </div>
        </div>
        <% }) %> <% } else { %>
        <h1>No Users</h1>
        <% } %>
      </div>
    </div>
  </body>
</html>
